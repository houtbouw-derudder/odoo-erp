<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- QWeb Reports -->
        <record id="quotation_document" model="ir.actions.report">
            <field name="name">Quotation</field>
            <field name="model">hdr_sales.quotation</field>
            <field name="report_type">qweb-pdf</field>
            <field name="report_name">hdr_sales.report_sales_quotation</field>
            <field name="report_file">hdr_sales.report_quotation</field>
            <!-- <field name="print_report_name">(object._get_report_base_filename())</field>
            <field name="attachment">(object.state == 'posted') and ((object.name or 'INV').replace('/','_')+'.pdf')</field> -->
            <field name="binding_model_id" ref="hdr_sales.model_hdr_sales_quotation"/>
            <field name="binding_type">report</field>
            <field name="groups_id" eval="[(4, ref('base.group_user')),(4, ref('base.group_user'))]"/>
        </record>
    </data>
</odoo>